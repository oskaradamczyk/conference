liip_imagine:
    resolvers:
        default:
            web_path: ~

    filter_sets :
        cache: ~

        thumb:
            quality: 75
            filters:
                thumbnail: { size : '%thumbnail_size%', mode : outbound }
